// shared with the workbox config, which imports this file directly: it must stay import-free,
// the path alias plugin does not apply while vite loads its own config
const mediaCacheName = 'mp3';

export default mediaCacheName;
